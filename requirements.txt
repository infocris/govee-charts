# Modern bleak on capable arches. On armv6l (Pi Zero / early Pi) there are no
# dbus-fast binary wheels and compiling it is impractical — pin a pure-Python
# dbus-fast + matching bleak instead.
bleak>=0.22.0; platform_machine != "armv6l"
bleak>=0.18.1,<0.19; platform_machine == "armv6l"
dbus-fast>=1.4.0,<1.18; platform_machine == "armv6l"
fastapi>=0.115.0
# uvicorn[standard] pulls uvloop, which often needs a native build on armv6l.
uvicorn[standard]>=0.32.0; platform_machine != "armv6l"
uvicorn>=0.32.0; platform_machine == "armv6l"
aiosqlite>=0.20.0
httpx>=0.27.0
tomli>=2.0.0; python_version < "3.11"
eval_type_backport>=0.2.0; python_version < "3.10"
